{% extends 'base.html.twig' %}

{% block body %}
    <h1>Statut</h1>

    <div class="row">
        <div class="col-md-2"><b>Identifiant : </b></div>
        <div class="col-md-8">{{ statut.idStatut }}</div>
    </div>
    <div class="row">
        <div class="col-md-2"><b>Libelle : </b></div>
        <div class="col-md-8">{{ statut.libelle }}</div>
    </div>
    <div class="row">
        <div class="col-md-2"><b>Actif : </b></div>
        <div class="col-md-8">{% if statut.actif %} Oui {% else %} Non {% endif %}</div>
    </div>


    <hr/>
    <a href="{{ path('statut_index') }}"><span class="glyphicon glyphicon-menu-hamburger"></span> Retour à la liste</a>
   <br/>
    <a href="{{ path('statut_edit', { 'idStatut': statut.idStatut }) }}"><span class="glyphicon glyphicon-pencil"></span> Modifier</a>
    <div class="pull-right">
        {{ form_start(delete_form) }}
            <input class="btn btn-primary" type="submit" value="Supprimer">
        {{ form_end(delete_form) }}
    </div>
{% endblock %}
