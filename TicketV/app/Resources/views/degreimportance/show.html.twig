{% extends 'base.html.twig' %}

{% block body %}
    <h1>Degreimportance</h1>

    <table>
        <tbody>
            <tr>
                <th>Libelle</th>
                <td>{{ degreImportance.libelle }}</td>
            </tr>
            <tr>
                <th>Actif</th>
                <td>{{ degreImportance.actif }}</td>
            </tr>
            <tr>
                <th>Iddegreimportance</th>
                <td>{{ degreImportance.idDegreImportance }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('degreimportance_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('degreimportance_edit', { 'idDegreImportance': degreImportance.idDegreImportance }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
